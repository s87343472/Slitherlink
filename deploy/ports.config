# Slitherlink项目端口配置
# 假设EC2上已有其他项目占用了常见端口

# 前端服务 (Next.js)
FRONTEND_PORT=3002

# 后端API服务 (Node.js)
BACKEND_PORT=8002

# 算法服务 (Java)
ALGORITHM_PORT=8082

# 数据库 (PostgreSQL) - 如果独立部署
DATABASE_PORT=5433

# Nginx反向代理端口
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443